apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  labels:
    strimzi.io/cluster: kafka-cluster # Change here to reflect the Kafka cluster name
  name: 3scale-policy-kafka-producer # Change here to change the resource name
  namespace: kafka # Change here if it is another namespace
spec:
  config: {}
  partitions: 1
  replicas: 3
  topicName: 3scale-policy-kafka-producer # Change here to change the resource name